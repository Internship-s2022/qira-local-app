export enum MainRoutes {
  LOGIN = '/login',
  STORYBOOK = '/storybook',
}

export enum AdminRoutes {
  ORDERS = '/orders',
  CLIENTS = '/clients',
  PRODUCTS = '/products',
  CATEGORY = '/categories',
  DELIVER = '/deliver',
}
