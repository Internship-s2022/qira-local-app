export interface FormattedProduct {
  id: string;
  category: string;
  name: string;
  currency: string;
  price: string;
  stock: number;
  isActive: boolean;
  logicDelete: boolean;
  state: string;
}
